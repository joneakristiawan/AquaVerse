import 'package:flutter/material.dart';

class DivePage extends StatelessWidget {
  const DivePage ({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Dive Page'),
    );
  }
}
